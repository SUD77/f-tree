<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Every permission the app asks for, and what each one is for. The first five are normal
        permissions: none of them prompts, and none of them is in the location family. The sixth,
        CAMERA, is the app's only runtime permission and is optional; see its own comment below.

        The first two exist solely for the optional updater, and nothing uses them until update
        checking is switched on in Settings. The next three exist solely for nearby sharing, and
        nothing uses them until somebody opens the nearby screen. Both are guarded the same way, in
        the code rather than in the interface: UpdateRepository will not make a request while its
        preference is false, and NearbyRepository will not bind a socket, join a group or send a
        datagram while its preference is false.

        There is still no sync, no account and no backend to have one with. What there is now is a
        way to hand one file to one device you can see, on a network you are already on, while you
        are looking at the screen that says so, and only after the other device says yes.

        Deliberately NOT NEARBY_WIFI_DEVICES. Plain multicast and a TCP socket are not
        location-deriving APIs, so choosing LAN over Wi-Fi Direct keeps this feature out of the
        location permission family entirely — which is a user-facing fact and not an implementation
        detail.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        The app's only runtime permission, and optional. Asked for when somebody taps "Scan the
        other screen's code" and at no other time, behind a rationale whose way out is "Type the
        code instead". Refusing it costs nothing: the list and the typed address both still work,
        and compare six digits. Not required as hardware, so a device without a camera still
        installs and simply does not show the button.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:name=".FTreeApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FTree">
        <!--
            One task, whichever way the app is entered.

            singleTask because opening a shared `.ftree` from a chat app should bring the reader to
            their own family tree with everything where they left it, not open a second copy of the
            app nested inside the chat app's task with a back button that leads somewhere strange.
            The file arrives at onNewIntent when the app is already running.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.FTree">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Opening a family tree somebody sent.

                Matched on type rather than on the `.ftree` name, because a content URI usually has
                no name in its path to match: a chat app stores what it received under a number and
                reports whatever type it recorded, which for an extension nothing has heard of means
                octet-stream. Matching those types is the only thing that puts this app in the list
                somebody is looking at when they tap a file a relative sent them.

                The cost is being offered for other files of no particular type, and it is paid
                openly: the file is read and refused with a sentence if it is not a family tree,
                rather than the app quietly not appearing when it should.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/x-zip-compressed" />
                <data android:mimeType="application/ftree" />
            </intent-filter>

            <!--
                Deliberately no `file:` filter, tempting though the `.ftree` name is to match on.
                A path in somebody's Downloads is not readable by this app on any current Android
                without a storage permission it has no other reason to hold, so claiming those files
                would mean appearing in the list and then failing — which is worse than not being
                there. Every file manager and chat app that matters hands over a content URI.
            -->

            <!-- Sending a file to this app from a chat app's share sheet. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/x-zip-compressed" />
                <data android:mimeType="application/ftree" />
            </intent-filter>
        </activity>

        <!--
            Hands the downloaded APK to the system installer as a content URI. Exported false with
            granted URI permissions, so the file is readable by the installer for the length of one
            intent and by nothing else.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_paths" />
        </provider>

        <!--
            Hands one person's exported branch to whichever app the user picked to send it with.
            Separate from the updater's provider so the two never share a directory: one holds an
            APK about to be installed, the other a piece of somebody's family about to leave the
            device, and they should not be one setting away from each other.
        -->
        <provider
            android:name=".transfer.ShareFileProvider"
            android:authorities="${applicationId}.shares"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/share_paths" />
        </provider>
    </application>

</manifest>
